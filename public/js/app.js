$( document ).ready(function () {
            $('body').panelSnap();

            var options = {
            $menu: $('div .menu')
          };

          $('.panel_container').panelSnap(options);
        });
